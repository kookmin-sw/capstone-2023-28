package com.capstone.traffic.ui.home

enum class PageType(val title: String, val tag: String) {
    PAGE1("board", "tag_board"),
    PAGE2("home", "tag_home"),
    PAGE3("route", "tag_route"),
    PAGE4("inform", "tag_inform")
}